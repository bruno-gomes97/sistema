import styled from 'styled-components';

export const Container = styled.div`
  padding: 2rem;
`;

export const Title = styled.h1`
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--primary);
  line-height: 120%;
  margin-bottom: 2rem;
`;
